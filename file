// int length = ptr_len(cmd->cmd);
// char **ptr = cmd->cmd;
// char **ptr2 = malloc(sizeof(char *) * length + 1);

// int D = 0;
// while (ptr[D])
// {
// 	ptr2[D] = ptr[D];
// 	D++;
// }
// ptr2[D++] = ft_strdup(" ");
// ptr[D] = NULL;
// cmd->cmd = ptr2;
// execve(cmd->cmd_path, cmd->cmd, NULL);
// exit(0);